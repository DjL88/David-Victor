node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local runtime persistence / diagnostics (may contain tenant or customer data)
.data/

# Runtime fallback snapshots are environment state, not source.
data/*.json
